export const metadata = {
    title: "Login",
    description: "Página de login do sistema Mandatuum",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
